/* global it describe*/

//import RouterHandler from './index';
import { expect } from 'chai';

describe('Add functionality test', () => {
    it('add(2,3) should returns 5', () => {
        //let calc = new RouterHandler();
        expect(2).to.be.equal(2);
    });
});
